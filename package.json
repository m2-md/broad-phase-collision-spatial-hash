{
  "name": "broad-phase-collision-spatial-hash",
  "version": "1.0.0",
  "description": "Broad-phase 2D collision detection: comparing naive O(n\u00b2) pairwise checks against spatial hash grid and quadtree spatial partitioning algorithms.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
