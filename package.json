{
  "name": "broad-phase-collision-spatial-hash",
  "version": "1.0.0",
  "description": "Broad-phase çarpışma: uzamsal hash ızgarası + quadtree ile O(n²) çift döngüyü neredeyse-doğrusala indirmek",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node src/bench-cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
